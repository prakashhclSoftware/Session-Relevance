import { Box } from '@mui/material';
import BarChart from './BarChart';

const Feed = () => {
  return (
    <Box sx={{ m: 2 }}>
        {/* <BarChart /> Needs to be fixed */}
        
    </Box>
  );
};

export default Feed